{
  "name": "elixir-workers",
  "version": "0.1.0",
  "private": true,
  "description": "Run Elixir on Cloudflare Workers via AtomVM WASI",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.63.0"
  }
}
