{
  "name": "elixir-workers",
  "version": "0.1.0",
  "private": true,
  "description": "Run Elixir on Cloudflare Workers via AtomVM WASI",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
